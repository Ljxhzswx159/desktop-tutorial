# 设计说明书

## 面向注塑成型过程的智能工艺参数优化与质量预测系统

| | |
|---|---|
| 课程名称 | 制造智能技术 |
| 设计题目 | 面向注塑成型过程的智能工艺参数优化与质量预测系统 |
| 文档版本 | V1.0 |
| 编制日期 | 2026 年 9 月 |

---

## 目录

- [1 概述](#1-概述)
- [2 需求分析](#2-需求分析)
- [3 总体设计](#3-总体设计)
- [4 数据设计](#4-数据设计)
- [5 算法设计](#5-算法设计)
- [6 接口设计](#6-接口设计)
- [7 界面设计](#7-界面设计)
- [8 测试设计](#8-测试设计)
- [9 部署与运行设计](#9-部署与运行设计)
- [附录 A 项目目录结构](#附录-a-项目目录结构)

---

## 1 概述

### 1.1 编写目的

本说明书规定"面向注塑成型过程的智能工艺参数优化与质量预测系统"的设计方案，包括系统的需求分析、总体结构、数据设计、算法设计、接口设计、界面设计、测试设计与部署方案，作为系统开发、测试、验收和维护的技术依据。本说明书依据《选题说明》《方案设计》两份前期文档编制，所有设计内容与代码实现严格对应。

### 1.2 项目背景

注塑成型是应用最广泛的塑料制品成型工艺之一，其工艺参数（熔体温度、注射压力、保压时间等）的设定长期依赖"老师傅的经验直觉"，试模一次失败即浪费数百元材料，调试周期长、成本高，经验难以沉淀与复用。本系统以轻量化、低成本为设计原则，面向中小企业注塑车间，将老师傅的经验转化为可复用、可优化的数据资产。

### 1.3 设计目标

1. **参数推荐**：输入产品类型、材料牌号、设备型号，输出 Pareto 最优工艺参数集；
2. **质量预测**：给定工艺参数组合，预测制品合格概率与缺陷类型；
3. **知识检索**：结构化注塑工艺知识，支持自然语言查询与缺陷根因分析；
4. **闭环学习**：记录老师傅的手动修正，反哺模型在线重训，系统持续进化。

### 1.4 术语与缩写

| 术语/缩写 | 说明 |
|---|---|
| NSGA-II | 非支配排序遗传算法（带精英策略的多目标进化算法） |
| Pareto 前沿 | 多目标优化中不可被同时改进的解集 |
| 双重诊断 | ML 模型预测 + 工艺规则诊断互为校验的诊断机制 |
| 能耗代理 | 以"循环时间 + 0.3×熔体温度"作为能耗的近似指标 |
| 训练-推理同源 | 合成数据打标签与在线诊断使用同一套规则引擎 |
| 在线学习 | 基于人工反馈触发模型重训并热加载的机制 |
| SPA | 单页应用（Single Page Application） |
| ORM | 对象关系映射（SQLAlchemy） |

### 1.5 设计依据

- 《制造智能技术课程设计任务书》及选题说明、方案设计文档；
- UCI 公开数据集 "Quality Prediction in a Plastic Injection Molding Process"（1451 条真实注塑工艺数据）；
- 注塑工艺领域知识：材料参数范围、缺陷机理与对策（教材 + 通用工艺经验）。

---

## 2 需求分析

### 2.1 用户角色

| 用户角色 | 核心诉求 |
|---|---|
| 工艺工程师 | 核心用户，负责工艺参数设定与质量管控，使用参数推荐、质量预测、反馈修正 |
| 车间操作员 | 查看推荐参数，录入实际生产数据 |
| 生产管理者 | 查看质量统计报表与设备运行状态（数据看板） |

### 2.2 功能需求

| 编号 | 模块 | 功能 | 输入 | 输出 |
|---|---|---|---|---|
| F1 | 参数推荐 | 基于输入条件推荐最优工艺参数 | 产品类型、材料牌号、设备型号 | 推荐参数 + Pareto 备选方案 + 历史案例 + 置信度说明 |
| F2 | 质量预测 | 预测给定参数下的制品质量 | 材料 + 13 个工艺参数 | 合格概率、缺陷类型及概率分布、规则诊断明细、根因对策 |
| F3 | 知识检索 | 工艺知识查询与经验复用 | 自然语言/关键词查询 | 知识条目 + 关联子图 + 缺陷根因分析 |
| F4 | 数据记录 | 生产数据录入与历史查询 | 工艺参数 + 检测结果 | 分页历史记录、统计信息 |
| F5 | 反馈修正 | 人工修正参数反向训练模型 | 原始参数 + 修正参数 + 修正后质量 | 反馈记录、模型重训结果、版本历史 |
| F6 | 数据看板 | 生产统计与质量趋势展示 | — | 聚合统计、图表数据 |

### 2.3 非功能需求

| 类别 | 需求 | 设计措施 |
|---|---|---|
| 性能 | 参数推荐单次响应约 2s | NSGA-II 适应度评估采用预构造 NumPy 模板行，避免反复构造 DataFrame |
| 可靠性 | 极端违规参数不被放行 | "模型 + 规则"双重诊断，任一命中即拦截 |
| 易用性 | 面向非 AI 专业用户 | 表单带参数范围提示，结果附置信度说明与工艺对策 |
| 可维护性 | 领域知识单一事实源 | 特征定义、材料参数范围集中于 `config.py`，各模块共享引用 |
| 可移植性 | 支持 SQLite / PostgreSQL 切换 | SQLAlchemy 抽象层，修改 `DB_URL` 即可切换 |
| 可部署性 | 离线可用、一键启动 | 前端依赖库全部本地化，`start.bat` 一键启动 |
| 可追溯性 | 模型演化可审计 | `model_versions` 表记录每次重训的样本量、反馈数与指标 |

---

## 3 总体设计

### 3.1 体系结构

系统采用 B/S 三层结构：浏览器端 SPA（表现层）→ FastAPI 应用（业务层，含算法服务层）→ SQLite/PostgreSQL + JSON 知识图谱（数据层）。前后端通过 RESTful API 以 JSON 交互，前端静态资源由后端同一服务托管，单进程即可运行完整系统。

```
┌─────────────────────────────────────────────────────┐
│          表现层  Vue3 + Element Plus + ECharts        │
│    参数推荐 │ 质量预测 │ 知识检索 │ 数据看板 │ 数据记录 │ 反馈修正 │
└──────────────────────────┬──────────────────────────┘
                           │ RESTful API (JSON)
┌──────────────────────────┴──────────────────────────┐
│               业务层  Python FastAPI                  │
│  recommend │ predict │ knowledge │ records │ feedback │ dashboard │
├──────────────────────────────────────────────────────┤
│                  算法服务层                           │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │ ModelService │ │ NSGA2        │ │ KnowledgeGraph│ │
│  │ XGBoost×2    │ │ Optimizer    │ │ 检索/根因分析  │ │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘  │
│         └──────── 三模块协同推荐 ────────┘            │
│  rule_diagnosis 规则诊断 │ feedback_service 在线重训   │
├──────────────────────────────────────────────────────┤
│           数据层                                       │
│  SQLAlchemy(SQLite 默认, 可切 PostgreSQL):            │
│    production_records │ feedback_records │ model_versions│
│  kg_data.json 知识图谱(JSON 内存图, 可导入 Neo4j)      │
│  dataset.csv 训练数据集 │ models/ 模型文件              │
└──────────────────────────────────────────────────────┘
```

### 3.2 模块划分

| 层 | 模块 | 文件 | 职责 |
|---|---|---|---|
| 表现层 | 根应用 | `frontend/assets/app.js` | 页面切换、组件注册、跨页通信总线 Inj |
| | 公共组件 | `frontend/assets/components.js` | ParamInputs 参数表单、DefectChart 缺陷图、推荐/预测/知识三页 |
| | 页面组件 | `frontend/assets/pages2.js` | 看板/记录/反馈三页 |
| 业务层 | 6 个路由 | `backend/app/routers/*.py` | 功能模块 API 端点 |
| | 请求/响应模型 | `backend/app/schemas.py` | Pydantic 数据校验 |
| | 全局配置 | `backend/app/config.py` | 特征定义、材料参数范围（领域知识单一事实源） |
| 算法层 | 模型服务 | `services/model_service.py` | XGBoost 模型加载（单例）、推理、热加载 |
| | 优化器 | `services/optimizer.py` | NSGA-II 多目标优化（DEAP） |
| | 推荐服务 | `services/recommend_service.py` | 三模块协同推荐流程编排 |
| | 规则诊断 | `services/rule_diagnosis.py` | 参数违规诊断（兼作合成数据打标器） |
| | 图谱服务 | `services/kg_service.py` | 知识图谱存储、检索、根因分析 |
| | 反馈服务 | `services/feedback_service.py` | 反馈积累与模型重训 |
| 数据层 | 数据访问 | `backend/app/db.py` | SQLAlchemy ORM 三张业务表 |
| | 离线脚本 | `backend/scripts/*.py` | 数据预处理、模型训练、演示数据播种 |

### 3.3 技术选型

| 层级 | 技术选型 | 说明 |
|---|---|---|
| 前端 UI | Vue3 + Element Plus + ECharts | CDN 库本地化至 `frontend/libs/`，免构建、离线可用 |
| 后端服务 | Python FastAPI + Uvicorn | RESTful API，自动生成 `/docs` 交互文档 |
| 机器学习 | XGBoost、scikit-learn | 质量二分类 + 缺陷八分类 |
| 智能优化 | DEAP（NSGA-II） | 多目标遗传算法 |
| 知识图谱 | JSON 内存图 + Neo4j 兼容 | 轻量内置检索，Cypher 脚本可一键导入 Neo4j |
| 数据库 | SQLAlchemy + SQLite | 默认零配置，可切 PostgreSQL |
| 测试 | unittest/urllib、jsdom、Playwright | 后端闭环、前端挂载、浏览器渲染三级验证 |

### 3.4 关键设计思想

**（1）三模块协同推荐。** 推荐流程由知识图谱、NSGA-II 优化、质量预测模型三大模块协同完成：图谱模块检索历史相似案例 → 优化模块在材料允许范围内生成候选参数 Pareto 集 → 预测模块在适应度函数内评估候选参数合格概率 → 综合选出最优推荐（详见 5.6）。

**（2）双重诊断机制。** ML 缺陷概率与知识图谱规则诊断互为校验：即使模型对极端违规参数漏判，规则引擎仍按领域知识（材料参数边界）拦截，保证"极端违规参数不被放行"。

**（3）训练-推理同源。** 合成数据的缺陷标签由 `rule_diagnosis.py` 同一套工艺规则引擎打标，保证训练标签规则与在线诊断规则语义一致，模型学到的决策边界与规则诊断对齐。

**（4）反馈在线学习闭环。** 操作员录入生产结果 → 老师傅修正参数提交反馈 → 反馈积累后触发增量重训 → 新样本并入训练集重训 XGBoost → 记录模型版本 → 模型热加载生效（详见 5.5）。

**（5）领域知识单一事实源。** 特征定义、7 种材料参数范围、缺陷类型等常量集中在 `config.py`，数据预处理、模型训练、参数优化、规则诊断、前端元信息全部由此引用，避免多模块知识不一致。

---

## 4 数据设计

### 4.1 工艺特征定义

13 个工艺特征与 UCI 真实数据集字段一一对应，是模型输入、记录存储、优化搜索空间的统一维度：

| # | 特征名 | 中文名 | 单位 | # | 特征名 | 中文名 | 单位 |
|---|---|---|---|---|---|---|---|
| 1 | melt_temp | 熔体温度 | °C | 8 | torque_peak | 扭矩峰值 | Nm |
| 2 | mold_temp | 模具温度 | °C | 9 | torque_mean | 扭矩均值 | Nm |
| 3 | time_to_fill | 填充时间 | s | 10 | back_pressure_peak | 背压峰值 | bar |
| 4 | plasticizing_time | 塑化时间 | s | 11 | injection_pressure_peak | 注射压力峰值 | bar |
| 5 | cycle_time | 循环时间 | s | 12 | screw_pos_end_hold | 保压结束螺杆位置 | mm |
| 6 | closing_force | 合模力 | kN | 13 | shot_volume | 射胶量 | cm³ |
| 7 | clamp_force_peak | 合模力峰值 | kN | | | | |

缺陷类型标签（多分类模型 8 类）：合格、翘曲变形、缩痕、飞边、银纹、熔接线、缺料、烧焦。

### 4.2 材料工艺参数范围（领域知识）

每种材料定义 **推荐范围（rec）** 与 **允许范围（bounds）** 两级区间。推荐范围用于前端默认值填充、合成数据采样与置信度说明；允许范围作为 NSGA-II 搜索边界与规则诊断的合规边界。6 个材料相关特征逐材料定义，其余 7 个机器参数采用全局典型范围。

| 材料 | 熔体温度 rec / bounds (°C) | 模具温度 rec / bounds (°C) | 填充时间 rec / bounds (s) | 注射压力峰值 rec / bounds (bar) | 循环时间 rec / bounds (s) |
|---|---|---|---|---|---|
| ABS | 215–235 / 190–250 | 55–65 / 40–80 | 5.5–7.5 / 4.5–9 | 820–950 / 760–1000 | 60–75 / 50–85 |
| PP | 220–250 / 200–280 | 35–45 / 20–80 | 5–7 / 4–8.5 | 700–830 / 650–880 | 55–70 / 45–80 |
| PC | 285–305 / 270–320 | 85–95 / 70–120 | 8–10 / 6.5–12 | 880–1010 / 820–1060 | 65–80 / 55–90 |
| PA66 | 265–280 / 260–290 | 70–80 / 60–90 | 7–9 / 5.5–10.5 | 840–970 / 780–1020 | 58–72 / 48–82 |
| POM | 195–210 / 180–220 | 75–85 / 60–100 | 6–8 / 5–9.5 | 780–910 / 720–960 | 58–72 / 48–82 |
| PS | 200–220 / 180–240 | 35–45 / 20–60 | 4.5–6.5 / 3.5–8 | 680–810 / 630–860 | 52–66 / 42–78 |
| PMMA | 225–245 / 210–260 | 60–70 / 50–80 | 7–9 / 5.5–10.5 | 800–930 / 740–980 | 60–74 / 50–84 |

全局参数范围（rec / bounds）：塑化时间 2.8–5.0 / 2.5–6.5 s；合模力 870–930 / 840–1000 kN；合模力峰值 890–950 / 860–1020 kN；扭矩峰值 95–140 / 85–175 Nm；扭矩均值 80–120 / 70–150 Nm；背压峰值 140–160 / 125–180 bar；保压结束螺杆位置 8.3–9.2 / 7.0–12.0 mm；射胶量 16–20 / 14–23 cm³（射胶量按材料定义，其余六项为全局）。

### 4.3 数据集设计

**（1）真实数据。** UCI 注塑质量预测数据集（1451 条），原始文件为分号分隔单列 Excel。处理规则：按分号拆分为 13 特征 + 质量标签；质量等级 {1,2}→不合格(0)，{3,4}→合格(1)；原数据未标注材料牌号，按 ABS 工艺归类；缺陷类型标注为"未知"，不参与缺陷多分类训练。

**（2）合成数据扩展。** 真实数据仅覆盖 ABS 一种材料且无缺陷标注，无法支撑缺陷类型预测与多材料推荐，故依据 4.2 节领域知识生成合成数据：

| 设计要素 | 取值 | 说明 |
|---|---|---|
| 规模 | 7 种材料 × 1000 条 = 7000 条 | 与真实数据合并后共 8451 条 |
| 违规特征采样 | 落在边界外违规区，深度为范围跨度的 30%（温度类 45%） | 温度类参数跨度大、典型操作错误偏离更远 |
| 非违规特征采样 | 70% 落推荐区、30% 落允许区 | 边界角落自然覆盖 |
| 违规概率 | 熔体温度 0.20、模具温度 0.18、填充时间/注射压力/循环时间 0.12、其余 0.05 | 保证每类缺陷有足够样本 |
| 违规策略 | 以单特征违规为主 | "某参数越界→不合格"决策边界清晰可学 |
| 缺陷标签 | `rule_diagnosis.py` 规则引擎打标 | 训练-推理同源（见 3.4） |
| 标签噪声 | 2% | 模拟检测误差 |

输出：`data/processed/dataset.csv`、`dataset_meta.json`（各材料样本量/合格率/特征范围）。

### 4.4 数据库设计

SQLAlchemy ORM 定义 3 张业务表，SQLite 默认（`DB_URL` 可切换 PostgreSQL），启动时自动建表。

**production_records（生产数据记录）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增主键 |
| created_at | DATETIME | 录入时间 |
| product_type / material / equipment / operator | VARCHAR(50/20/50/50) | 产品、材料、设备、操作员 |
| melt_temp … shot_volume | FLOAT ×13 | 13 个工艺特征 |
| quality | INTEGER | 0 不合格 / 1 合格 |
| defect_type | VARCHAR(20) | 缺陷类型（空表示合格/未检） |
| source | VARCHAR(20) | manual 人工录入 / recommended 执行推荐 / corrected 修正后执行 |
| note | VARCHAR(200) | 备注 |

**feedback_records（人工修正反馈）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id / created_at | INTEGER PK / DATETIME | 主键、提交时间 |
| material / operator | VARCHAR | 材料、提交人 |
| original_params / corrected_params | TEXT(JSON) | 原始参数、老师傅修正参数 |
| quality_after / defect_after | INTEGER / VARCHAR | 修正后质量、缺陷 |
| reason | VARCHAR(200) | 修正原因 |
| applied | BOOLEAN | 是否已用于重训（默认 False） |

**model_versions（模型版本历史）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id / trained_at | INTEGER PK / DATETIME | 主键、训练时间 |
| samples | INTEGER | 训练样本总量 |
| feedback_used | INTEGER | 本次使用的反馈条数 |
| metrics | TEXT(JSON) | accuracy / f1 / auc |
| note | VARCHAR(200) | 备注（"反馈在线学习"） |

### 4.5 知识图谱设计

知识图谱存储于 `knowledge/kg_data.json`（教材知识 + 通用注塑工艺经验整理），系统内置轻量 JSON 内存图检索，同时自动生成 Neo4j 导入脚本 `data/processed/knowledge_graph.cypher`，可一键迁移至图数据库。

**实体（43 个，6 类）**

| 实体类型 | 数量 | 内容 | 属性示例 |
|---|---|---|---|
| 材料 | 7 | ABS、PP、PC、PA66、POM、PS、PMMA | 全称、熔体/模具温度范围、干燥条件、收缩率、特性 |
| 设备 | 4 | 海天MA1200/MA2500、震雄SM120、伊之密UN260 | 锁模力、螺杆直径、理论注射容量、注射压力上限 |
| 参数 | 7 | 模具温度、熔体温度、注射压力、注射速度、保压压力、保压时间、冷却时间 | 单位、典型范围、影响方向 |
| 缺陷 | 10 | 翘曲变形、缩痕、飞边、银纹、熔接线、气泡、缺料、烧焦、裂纹、尺寸偏差 | 描述、主要诱因 |
| 对策 | 9 | 延长冷却并均衡模温、提高保压、降低注射压力速度、严格干燥、提高/降低模温、改善排气、退火处理等 | 操作要点 |
| 工艺规则 | 6 | 保压时间、冷却时间占比、熔体温度范围、模具温度、注射压力裕量、保压压力设定规则 | 规则内容 |

**关系（51 条，4 类）**

| 关系 | 语义 | 方向 | 数量 |
|---|---|---|---|
| 适用于 | 设备适配材料 | 设备 → 材料 | 8 |
| 影响 | 参数对缺陷的诱发关系 | 参数 → 缺陷 | 20 |
| 解决 | 对策消除缺陷 | 对策 → 缺陷 | 17 |
| 约束 | 工艺规则约束参数 | 规则 → 参数 | 6 |

关键关系模式：缺陷根因分析依赖 `(参数)-[影响]->(缺陷)<-[解决]-(对策)` 两跳子图，从任意缺陷实体即可检索全部诱因参数与操作对策。

---

## 5 算法设计

### 5.1 质量预测模型（机器学习）

**（1）质量二分类模型。** 输入 13 个工艺特征 + 材料 one-hot 编码（7 维，共 20 维特征），输出合格概率。XGBoost 与随机森林对比训练，5 折分层交叉验证评估（`scripts/train.py`），XGBoost 全面胜出被选用：

| 模型 | 测试集准确率 | F1 | ROC-AUC | 5 折 CV AUC |
|---|---|---|---|---|
| **XGBoost（选用）** | **0.9432** | **0.9394** | **0.9769** | **0.9763±0.0031** |
| 随机森林 | 0.8652 | 0.8622 | 0.9418 | 0.9391±0.0055 |

XGBoost 超参数：n_estimators=300、max_depth=6、learning_rate=0.08、subsample=0.9、colsample_bytree=0.9。特征重要性 Top5：熔体温度（0.1288）、材料 POM 标识（0.0992）、循环时间（0.0920）、材料 PC 标识（0.0844）、模具温度（0.0722）——温度类参数主导质量，与注塑工艺经验一致。

**（2）缺陷八分类模型。** 基于 7000 条合成数据训练（真实数据缺陷未知不参与），XGBoost n_estimators=400、max_depth=7、eval_metric=mlogloss，LabelEncoder 编码 8 类标签，测试集准确率 **0.8979**、macro-F1 **0.8265**。

**（3）模型服务封装。** `ModelService` 单例在进程启动时加载两个模型与元信息，避免每次请求重复反序列化；推理时以特征字典 + 材料 one-hot 构造输入行。重训完成后调用 `ModelService.reload()` 热加载新模型。

### 5.2 NSGA-II 参数优化（智能优化）

基于 DEAP 实现 NSGA-II 多目标遗传算法（`services/optimizer.py`）。

| 设计要素 | 设计 |
|---|---|
| 优化目标 | ① 最大化质量合格概率（由 XGBoost 模型评估）；② 最小化能耗代理指标 `E = 循环时间 + 0.3×熔体温度` |
| 决策变量 | 13 个工艺特征，逐特征限定在该材料允许范围（bounds）内，有界约束 |
| 编码 | 实数编码，个体为 13 维浮点向量 |
| 交叉 | 模拟二进制交叉 SBX（η=15），越界截断 |
| 变异 | 多项式变异（η=20，indpb=0.35），越界截断 |
| 选择 | NSGA-II 非支配排序选择（selNSGA2），μ+λ 策略（eaMuPlusLambda） |
| 运行参数 | 种群 80、进化 30 代（API 默认）；cxpb=0.8、mutpb=0.2（满足 DEAP 概率和 ≤1 约束） |
| 性能优化 | 预构造 NumPy 模板行（仅材料列置 1），适应度评估时仅填充 13 个特征值，避免反复构造 DataFrame，单次推荐约 2s |

**推荐解选取策略**：从 Pareto 前沿取合格概率 ≥ 前沿最高值 99.5%（`best - 0.005`）的解中能耗最低者——质量优先、兼顾能耗；并附规则诊断结果（`rule_check`）与置信度说明（模型 AUC、推荐区间内参数比例 `in_rec_ratio`）。

### 5.3 工艺规则缺陷诊断

`services/rule_diagnosis.py` 基于材料参数允许范围定义 7 类违规规则，违规明细按严重度排序（`RULE_ORDER = 烧焦 > 银纹 > 缺料 > 飞边 > 熔接线 > 缩痕 > 翘曲变形`），多条违规时取严重度最高的缺陷为首要缺陷：

| # | 违规条件 | 缺陷 | # | 违规条件 | 缺陷 |
|---|---|---|---|---|---|
| 1 | 熔体温度 > 上限 | 银纹/烧焦（随机 0.55/0.45） | 6 | 填充时间 > 上限 | 熔接线 |
| 2 | 熔体温度 < 下限 | 缺料 | 7 | 注射压力峰值 > 上限 | 飞边 |
| 3 | 模具温度 > 上限 | 缩痕 | 8 | 注射压力峰值 < 下限 | 缺料 |
| 4 | 模具温度 < 下限 | 熔接线 | 9 | 循环时间 < 下限（冷却不充分） | 翘曲变形 |
| 5 | 填充时间 < 下限（过快） | 银纹 | 10 | 保压结束螺杆位置 < 下限（补缩不足） | 缩痕 |

该模块承担三重职责：合成数据打标签（训练-推理同源）、API 在线诊断、推荐解合规校验。

### 5.4 知识图谱检索

`services/kg_service.py` 轻量内存图存储（节点字典 + 邻接表），支持：

- **三级实体匹配**：精确 ID → 名称相等 → 名称包含（如"翘曲"命中"翘曲变形"）→ 口语别名映射（"料花"→银纹、"打不满"→缺料、"亚克力"→PMMA 等 30+ 别名）；
- **类型加权检索**：关键词提示实体类型（"怎么办"→对策、"注塑机"→设备），名称匹配 +1.0、属性匹配 +0.5、类型命中 +0.3、别名命中 +0.8，按得分排序；
- **自然语言整句根因分析**：查询中出现缺陷（名称与查询互为子串或别名命中）时，自动附加 `(参数)-[影响]->(缺陷)<-[解决]-(对策)` 两跳根因分析结果；
- **材料工艺画像**：材料 + 适用设备（"适用于"关系）+ 全部工艺规则；
- **子图导出**：任意实体一跳子图供前端 ECharts 图谱可视化，全图导出接口供画布渲染（6 类实体分类着色）。

### 5.5 反馈在线学习

`services/feedback_service.py` 实现"反馈积累 → 增量重训 → 版本可追溯"闭环：

1. **样本构造**：遍历未应用（applied=False）的反馈，修正后合格 → 修正参数作为正样本；修正后不合格 → 修正参数作为负样本；同时将原始参数按修正结果反向标记（修正后合格说明原始参数有问题 → 负样本，反之 → 负样本）并入训练集；
2. **模型重训**：合并反馈样本后的全量数据按 8:2 分层划分，XGBoost（超参数同 5.1）重训，评估 accuracy / f1 / auc；
3. **持久化与版本**：模型写回 `quality_model.joblib`，更新 `model_meta.json`（指标、样本量），`model_versions` 表记录版本，反馈标记 applied=True；
4. **热加载**：`ModelService.reload()` 使新模型即时生效。

### 5.6 参数推荐协同流程

```
用户输入(产品/材料/设备)
  ① 图谱模块检索历史相似案例:
     生产记录库合格记录(最近优先) → 老师傅修正反馈(合格)
     → 数据集合格样本(按与推荐范围中心欧氏距离取近邻), 共 5 条
  ② 优化模块: NSGA-II 在该材料允许范围内生成 Pareto 前沿(种群80×30代)
  ③ 预测模块: 适应度函数内 XGBoost 评估每个候选的合格概率
  ④ 综合输出:
     推荐参数(质量优先兼顾能耗) + Pareto 备选(前12个, 附规则诊断)
     + 历史案例 + 置信度(合格概率/模型AUC/推荐区间占比) + 材料工艺画像
```

---

## 6 接口设计

### 6.1 接口约定

- 风格：RESTful，资源路径 + JSON 请求/响应体；请求模型由 Pydantic 定义并自动校验；
- 统一前缀：功能接口 `/api/*`，系统接口 `/api/health`、`/api/meta`；
- 交互式文档：FastAPI 自动生成 Swagger UI（`/docs`）；
- CORS：放开跨域（本地部署场景）。

### 6.2 接口清单

| 方法 | 路径 | 请求体/参数 | 响应要点 |
|---|---|---|---|
| POST | /api/recommend | `{product_type, material, equipment}` | `recommended`（参数+合格概率+能耗+规则校验+模型名）、`confidence`（概率/AUC/推荐区间占比）、`pareto_front[≤12]`、`history_cases[≤5]`、`material_profile` |
| POST | /api/predict/quality | `{material, params(13特征)}` | `quality`、`quality_prob`、`defect_type`、`defect_distribution[8类]`、`rule_diagnosis`（违规明细）、`defect_analysis`（根因+对策+子图） |
| POST | /api/predict/defect | `{material, params}` | `defect_type`、`distribution`（8 类概率降序） |
| GET | /api/predict/model/info | — | 模型名、质量/缺陷模型指标、特征重要性 |
| POST | /api/knowledge/search | `{query, limit=10}` | `results[]`（命中实体+得分+一跳邻居）、`defect_analysis`（查询含缺陷时自动附带） |
| POST | /api/knowledge/diagnose | `{material, params}` | `diagnosis`（缺陷类型+违规明细）、`analysis`（根因对策） |
| GET | /api/knowledge/material/{material} | 路径参数 | 材料节点 + 适用设备 + 工艺规则（未知材料 404） |
| GET | /api/knowledge/materials | — | 7 种材料完整 rec/bounds 范围 |
| GET | /api/knowledge/graph | — | 全图节点+边（前端图谱画布） |
| POST | /api/records | RecordCreate（13 特征+质量+缺陷+来源+备注） | 创建后的完整记录 |
| GET | /api/records | `?material=&page=1&page_size=20` | `total`、`items[]`（按时间倒序分页） |
| GET | /api/records/stats | — | 总量、合格率、按材料/缺陷/日期分布（看板数据源） |
| POST | /api/feedback | `{material, original_params, corrected_params, quality_after, defect_after, reason}` | 反馈 id、applied 状态 |
| GET | /api/feedback | — | 反馈列表（含 applied 标记） |
| POST | /api/feedback/retrain | — | `retrained`、`feedback_used`、`samples`、`metrics` |
| GET | /api/feedback/models | — | 模型版本历史（时间/样本量/反馈数/指标） |
| GET | /api/dashboard | — | `record_stats` + `model`（指标/特征重要性/版本趋势）+ 材料/缺陷标签 |
| GET | /api/health | — | 服务状态与版本 |
| GET | /api/meta | — | 材料/产品/设备下拉选项、当前模型名 |

### 6.3 关键接口示例

**POST /api/recommend**

```json
// 请求
{ "product_type": "电子外壳", "material": "PC", "equipment": "伊之密UN260" }
// 响应(节选)
{
  "recommended": {
    "params": { "melt_temp": 293.42, "mold_temp": 91.07, "cycle_time": 71.56, "...": "..." },
    "quality_prob": 0.9876,
    "energy_proxy": 159.59,
    "rule_check": "合格",
    "model": "xgboost"
  },
  "confidence": {
    "quality_prob": 0.9876, "model_auc": 0.9769, "in_rec_ratio": 1.0,
    "note": "合格概率由XGBoost模型评估;能耗代理=循环时间+0.3×熔体温度"
  },
  "pareto_front": [ { "params": {...}, "quality_prob": 0.99, "energy": 158.1, "rule_check": "合格", "violations": [] } ],
  "history_cases": [ { "source": "生产记录 #12", "equipment": "伊之密UN260", "params": {...}, "quality": 1 } ],
  "material_profile": { "label": "PC(聚碳酸酯)", "rec_ranges": {...}, "equipments": ["伊之密UN260"], "rules": [...] }
}
```

**POST /api/predict/quality**

```json
// 请求(节选)
{ "material": "ABS",
  "params": { "melt_temp": 255, "mold_temp": 60, "time_to_fill": 6.5,
              "cycle_time": 70, "injection_pressure_peak": 900, "shot_volume": 18, "...": "..." } }
// 响应(节选: 熔体温度 255°C 超出 ABS 上限 250°C)
{
  "quality": 0, "quality_prob": 0.0231, "defect_type": "烧焦",
  "defect_distribution": [ { "defect": "烧焦", "prob": 0.86 }, "...": "..." ],
  "rule_diagnosis": {
    "defect_type": "烧焦",
    "violations": [ { "param": "melt_temp", "rule": "熔体温度超过上限 250°C", "defect": "烧焦", "severity": 0 } ]
  },
  "defect_analysis": {
    "defect": "烧焦",
    "causes": [ { "param": "熔体温度", "detail": "熔体温度过高导致烧焦" }, {"...": "..."} ],
    "countermeasures": [ { "action": "改善模具排气", "detail": "加深排气槽;清理排气口堵塞" }, {"...": "..."} ]
  }
}
```

---

## 7 界面设计

前端为免构建 SPA（`frontend/index.html`），左侧固定导航栏 + 右侧主内容区，6 个页面组件按 `v-show` 切换；Vue、Element Plus、ECharts、axios 全部本地化，由后端静态托管；全局通信总线 `Inj`（挂载于 `app.config.globalProperties`）支持跨页数据传递（如推荐结果一键应用到质量预测页）。

| 页面 | 布局与交互设计 |
|---|---|
| 参数推荐 | 表单（产品类型/材料/设备下拉，材料切换后表单默认值按推荐范围中值联动）→ 推荐参数卡片（逐参数展示+规则校验结果）→ Pareto 前沿散点图 → 历史案例表 → 材料工艺画像；"一键应用"按钮将推荐参数填入质量预测页 |
| 质量预测 | 13 参数表单（每项带推荐范围提示）→ 合格概率仪表 + 缺陷概率分布条形图（DefectChart）→ 规则诊断违规明细表 → 图谱根因对策卡片 |
| 知识检索 | 搜索框（支持自然语言）→ 知识卡片列表（实体+一跳邻居关系）→ 根因分析卡片（诱因+对策）→ 知识图谱可视化画布（6 类实体分类着色，节点可点击展开子图） |
| 数据看板 | 5 个 ECharts 画布：质量趋势（合格/不合格/记录数折线）、缺陷分布（饼图）、材料合格率（柱图）、特征重要性（条形图）、模型版本趋势（准确率折线） |
| 数据记录 | 录入表单（13 参数+质量+缺陷+来源）→ 历史记录分页表格（按材料过滤） |
| 反馈修正 | 从生产记录选择或手动填写原始/修正参数 → 参数差异对比表 → 提交反馈 → 触发在线重训 → 展示重训结果与模型版本历史 |

配色遵循数据可视化规范：6 类图谱实体使用分类色（经色觉缺陷可读性校验）；侧边栏品牌区含系统图标，整体为企业管理后台风格。

---

## 8 测试设计

### 8.1 测试策略

三级测试体系，覆盖"算法单元 → API 业务闭环 → 前端真实渲染"：

| 层级 | 工具 | 测试对象 |
|---|---|---|
| 后端端到端闭环 `tests/test_api_e2e.py` | 标准库 urllib（零额外依赖） | 完整业务链路：推荐 → 预测 → 记录 → 反馈 → 重训 → 看板 |
| 前端挂载 `tests/frontend_mount_test.js` | jsdom | 加载真实 vue/element-plus/组件文件，挂载根应用并逐页切换，捕获运行时错误（8 个组件模板编译 + 6 页面挂载） |
| 浏览器渲染 `tests/browser_screenshot.js` | Playwright + Chromium | 6 页面真实渲染 + 关键交互断言 + 控制台错误检查，输出截图 |

### 8.2 端到端关键断言（20 项）

1. **参数推荐**：返回 13 维推荐参数；推荐合格概率 > 0.8；Pareto 前沿非空；历史案例非空；材料画像含所选设备；
2. **质量预测**：推荐参数预测合格；模型对典型违规参数独立判定不合格；极端违规参数不被放行（模型+规则任一命中即拦截）；规则诊断给出违规明细；图谱给出根因对策；
3. **数据记录**：创建记录、按材料查询；
4. **反馈在线学习**：提交反馈 → 触发重训 → 重训后 AUC > 0.9，模型版本可追溯；
5. **知识检索**：自然语言整句查询（"翘曲变形了怎么办"）触发根因分析；关键词检索命中；
6. **看板**：聚合统计正常、模型版本趋势非空。

### 8.3 集成调试记录（关键问题与对策）

| 问题 | 对策 |
|---|---|
| DEAP 交叉+变异概率和超限运行时报错 | 调整为 cxpb=0.8、mutpb=0.2 并写入测试防回归 |
| NSGA-II 评估反复构造 DataFrame，推荐耗时过长 | 预构造 NumPy 模板行，单次推荐降至约 2s |
| 知识图谱关系方向匹配错误导致根因分析查不到诱因 | 统一 `(参数)-[影响]->(缺陷)` 方向语义 |
| 模型对极端违规参数漏判 | 合成数据违规深度提高至 45%（温度类）、以单特征违规为主；断言升级为双重诊断语义 |
| 前端构建模板无法访问全局变量 | Inj 挂载 `app.config.globalProperties` |
| 自然语言整句查询无法命中缺陷 | 缺陷名与查询互为子串 + 别名三级匹配 |

---

## 9 部署与运行设计

### 9.1 运行环境

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10/11（开发验证环境） |
| Python | 3.13 |
| 依赖库 | fastapi、uvicorn、pydantic、sqlalchemy、numpy、pandas、scikit-learn、xgboost、joblib、openpyxl、deap（见 `backend/requirements.txt`） |
| 浏览器 | Chrome/Edge 等现代浏览器（前端无构建步骤） |
| 数据库 | SQLite（默认零配置）/ PostgreSQL（可选） |

### 9.2 初始化流程

```bash
# 1. 安装依赖(清华镜像)
py -3.13 -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r backend/requirements.txt

# 2. 数据预处理(真实数据解析 + 合成数据生成 + Cypher 导出)
cd backend && py -3.13 scripts/preprocess.py

# 3. 模型训练(XGBoost/RF 对比 + 缺陷多分类)
py -3.13 scripts/train.py

# 4. (可选)演示数据播种(近14天模拟生产记录)
py -3.13 scripts/seed_demo_data.py
```

### 9.3 启动方式

```bash
# 方式一: 命令行启动(自动托管前端)
py -3.13 -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 方式二: Windows 一键启动(根目录 start.bat / open_system.bat, 自动打开浏览器)
```

启动后访问 `http://localhost:8000`（系统页面）、`http://localhost:8000/docs`（API 文档）。应用启动时自动建库；模型文件与元信息从 `data/processed/models/` 加载。

---

## 附录 A 项目目录结构

```
keshe/
├── backend/                     # Python FastAPI 后端
│   ├── app/
│   │   ├── main.py              # 应用入口(API + 前端静态托管)
│   │   ├── config.py            # 全局配置: 特征定义、材料参数范围(领域知识)
│   │   ├── db.py                # SQLAlchemy 数据层(SQLite 默认, 可切 PostgreSQL)
│   │   ├── schemas.py           # Pydantic 请求/响应模型
│   │   ├── routers/             # 6 个功能模块 API
│   │   │   ├── recommend.py     #   参数推荐
│   │   │   ├── predict.py       #   质量预测
│   │   │   ├── knowledge.py     #   知识检索
│   │   │   ├── records.py       #   数据记录
│   │   │   ├── feedback.py      #   反馈修正(在线学习)
│   │   │   └── dashboard.py     #   数据看板
│   │   └── services/            # 算法服务层
│   │       ├── model_service.py       # 模型加载与推理(单例+热加载)
│   │       ├── optimizer.py           # NSGA-II 多目标优化(DEAP)
│   │       ├── kg_service.py          # 知识图谱存储与检索
│   │       ├── rule_diagnosis.py      # 工艺规则缺陷诊断
│   │       ├── recommend_service.py   # 三模块协同推荐流程
│   │       └── feedback_service.py    # 反馈在线学习(模型重训)
│   └── scripts/
│       ├── preprocess.py        # 数据预处理(真实数据解析 + 合成数据生成)
│       ├── train.py             # 模型训练(XGBoost/RF 对比 + 缺陷多分类)
│       └── seed_demo_data.py    # 演示数据播种(近14天模拟记录)
├── frontend/                    # Vue3 + Element Plus(CDN 本地化, 免构建)
│   ├── index.html               # SPA 骨架(6 个页面)
│   ├── assets/                  # components.js / pages2.js / app.js / style.css
│   └── libs/                    # vue / element-plus / echarts / axios(离线可用)
├── knowledge/kg_data.json       # 注塑工艺知识图谱数据(6 类实体、4 类关系)
├── data/
│   ├── raw/                     # 原始数据(UCI 注塑质量预测数据集)
│   └── processed/               # dataset.csv / dataset_meta.json / models/ /
│                                # injection.db / knowledge_graph.cypher
├── tests/                       # 后端闭环测试 + 前端挂载测试 + 浏览器渲染验证
├── docs/                        # 选题说明、方案设计、设计报告、本设计说明书
├── prompt/                      # AI 会话过程档案(JSON)
├── start.bat / open_system.bat  # Windows 一键启动
└── README.md                    # 项目说明(架构图 + 目录说明)
```

---

*本说明书与代码实现一一对应；实现变更时需同步修订本文档。*
